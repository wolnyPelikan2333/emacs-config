;; ============================
;; Org core
;; ============================

(require 'org)

(setq org-adapt-indentation t
      org-startup-indented t)

(setq org-default-notes-file "~/mapa/sesja.org")

(provide 'org-core)
