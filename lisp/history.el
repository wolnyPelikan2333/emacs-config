;; ============================
;; History / recent files
;; ============================

(savehist-mode 1)
(setq history-length 1000)

(recentf-mode 1)
(setq recentf-max-saved-items 300)

(provide 'history)
